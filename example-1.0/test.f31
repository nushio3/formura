answer = 6 * 7
(x) = 3
fact = for(x) x * fact(x-1)
fix = for (f)  f(fix(f))
b[i+1, j] = (a+b)[i]
big = let a = 1; b = 2; int :: c in A + B + C
double :: a , b = 3, c
== = ==


ddx = for(a) (a[i+1] - a[i-1])/2

∂ = (ddx,ddy,ddz)

dvdt = SIGMA for(j) ∂ j . p j


dvdt = Σ (for (j) ∂(j) (∂(i) (p(j))))
kisho'joho'shori = hawai'i


d = for(j) (∂ j) * (∂ i) * p k
# 関数適用が左結合で、ピリオドが右結合なら、ふつうにこれで行けるんじゃね？
dvdt = SIGMA for(j) P j . P i . p j

begin function y = square(x)
    y = x ** 2
end function

begin function square(x) returns y
    y = x ** 2
end function

# begin function wrong(x+y) returns z
#     y = x ** 2
# end function
